Run these tonight on the home PC. No guessing.

1) Bootstrap (Admin PowerShell)
powershell -ExecutionPolicy Bypass -File "C:\GSADUs\Dev\Powershell\Install-HomePC-Bootstrap.ps1"


Expected: SSH test prints GitHub greeting or a key to add, repo lands at C:\GSADUs\Dev, tasks GSADUs_AutoPull_OnLogon and GSADUs_AutoPull_2h created.

2) If SSH says “permission denied”

Add the printed public key at GitHub → Settings → SSH and GPG keys, then:

ssh -T git@github.com

3) Open and build

Open C:\GSADUs\Dev\GSADUs.Revit.BatchExport.sln in VS.

Build → Rebuild Solution.

If your Revit version is not 2026, edit:

C:\GSADUs\Dev\src\GSADUs.Revit.Addin\GSADUs.Revit.Addin.csproj


Update the two <HintPath> entries to the installed Revit year, then rebuild.

4) Install the add-in files
powershell -ExecutionPolicy Bypass -File "C:\GSADUs\Dev\Powershell\Install-GSADUsAddin.ps1" -Force


Expected output shows .addin under C:\ProgramData\Autodesk\Revit\Addins\{year} and DLL in C:\GSADUs\RevitAddin.

5) Verify Git path
git -C C:\GSADUs\Dev status -sb
git -C C:\GSADUs\Dev pull --rebase --autostash

6) Quick push test from home
echo "home ok $(Get-Date -Format s)" >> C:\GSADUs\Dev\DEVNOTES.md
git -C C:\GSADUs\Dev add DEVNOTES.md
git -C C:\GSADUs\Dev commit -m "chore: home ok"
git -C C:\GSADUs\Dev push

7) VS settings (set once on home PC)

Git: Rebase on pull = Yes, Prune on fetch = Yes.

Documents: Auto-detect external changes = On, Reload modified files = On.

8) Scheduled tasks check
Get-ScheduledTask GSADUs_AutoPull_OnLogon,GSADUs_AutoPull_2h | Get-ScheduledTaskInfo |
  Select-Object TaskName, LastRunTime, LastTaskResult


LastTaskResult should be 0.

If any step deviates, paste the exact command and output.