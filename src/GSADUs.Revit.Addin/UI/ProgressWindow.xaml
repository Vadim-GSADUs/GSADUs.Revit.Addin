<Window x:Class="GSADUs.Revit.Addin.UI.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Progress" Width="460" Height="170"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock x:Name="HeaderText" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap"
               Text="Exporting..."/>

    <ProgressBar x:Name="OverallBar" Grid.Row="1" Height="18" Margin="0,8,0,0"
                 Minimum="0" Maximum="100" Value="0"/>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,8,0,0">
      <TextBlock x:Name="ElapsedText" Text="Elapsed: 00:00:00"/>
      <TextBlock Text="  " />
      <TextBlock x:Name="StatusText" Foreground="Gray"/>
    </StackPanel>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button x:Name="CancelBtn" Width="90" Height="26" Content="Cancel" Click="CancelBtn_Click"/>
    </StackPanel>
  </Grid>
</Window>
